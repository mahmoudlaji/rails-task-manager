<h1>All Tasks to Do!!!!</h1>

<ul>
    <% @tasks.each do |task| %>
        <li>
            <%= link_to task.title, task_path(task) %>
        </li>
        <% end %>
        <%= link_to "New Task To Create", new_task_path  %>
</ul>